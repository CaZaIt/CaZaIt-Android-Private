<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CaZaIt" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/main_beige</item>
        <item name="android:navigationBarColor">@color/main_black</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.CaZaIt" parent="Base.Theme.CaZaIt" />

    <!-- Splash -->
    <style name="Theme.CaZaIt.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_cazait_splash</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.CaZaIt</item>
        <item name="windowSplashScreenBackground">@color/main_beige</item>
    </style>

    <style name="Theme.CaZaIt.CafeList" parent="Theme.MaterialComponents.DayNight.NoActionBar">

    </style>
</resources>