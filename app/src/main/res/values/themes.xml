<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CaZaIt" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CaZaIt" parent="Base.Theme.CaZaIt" />

    <!-- Splash -->
    <style name="Theme.CaZaIt.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_cazait_splash</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.CaZaIt</item>
        <item name="windowSplashScreenBackground">@color/main_beige</item>
    </style>
</resources>